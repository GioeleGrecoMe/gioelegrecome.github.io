Room Scanner v9.5.1 Hotfix5W6 - source overlay asset status

This generated source overlay does NOT embed the large ONNX/WASM binaries.
That is intentional and is not reported as FULL_LOCAL_READY.

Apply this ZIP over the existing deployed project while preserving the existing
models/ and vendor/ binaries, or run the fetch scripts before publishing.

H5W6 then verifies:
- MobileSAM encoder/decoders: pinned byte count + SHA-256 before ORT session use.
- MobileSAM: validated input/output graph contract + real encoder->decoder smoke inference.
- Depth Anything Q4F16: pinned byte count + SHA-256 in its worker.
- Depth Anything: validated NCHW contract + real smoke inference + finite output check.
- WASM override paths: absolute URLs.

A complete local tree must pass:
  python3 tools/check_deploy_bundle.py
and report:
  FULL_LOCAL_READY=yes
