ROOM SCANNER V11-D13 - TEST REPORT
Build: 2026-08-16

DIAGNOSI DAL LOG D12 REALE
- Il click su v10Start parte con userActivation.isActive=true.
- La patch esterna D12 registra canToggle=false: CFG.depthAIEnabled non e' accessibile dal file esterno.
- v10RenderPhotoReview e' undefined dal file esterno: anche il guard foto D12 non puo' sostituirla.
- Il primo requestSession arriva 15.864 s dopo il click, con userActivation.isActive=false.
- Chrome restituisce SecurityError: immersive-ar richiede user activation.
- Il secondo tentativo arriva circa 19.9 s dopo il click e fallisce nello stesso modo.

CORREZIONE D13
- Il loader modifica il testo V10 prima dell'esecuzione.
- Inserisce un prologo direttamente dentro v10StartScanWithDepthCheck, nello stesso lexical scope di CFG.
- Il prologo mette CFG.depthAIEnabled=false soltanto durante l'ingresso XR.
- Il tracer XR richiede il restore soltanto dopo xr-requestSession-ok.
- Il restore e' ritardato di 3000 ms per dare priorita' alla stabilizzazione della sessione XR.
- Dopo il restore, ensureDepthAIWorker viene invocato nello scope V10.
- v10RenderPhotoReview riceve un source guard temporaneo che ritorna subito, evitando il ReferenceError rgb.

TEST STATICI
PASS node --check room_scanner_v11_d13_loader.js
PASS node --check room_scanner_v11_d13_xrtrace.js
PASS node --check room_scanner_v11_d13_diag.js
PASS node --check depth_ai_worker_v11_d13.js
PASS worker D12->D13 identico dopo normalizzazione nomi versione

TEST SOURCE-PATCH / USER ACTIVATION
Fixture:
- CFG.depthAIEnabled e' lexical, non window globale.
- Il precheck Depth simulato dura 150 ms.
- La user activation simulata scade dopo 100 ms.
Risultato D13:
- requestSession raggiunta in circa 1 ms.
- risultato XR preservato.
- CFG.depthAIEnabled ripristinato true dopo sessione XR.
- ensureDepthAIWorker invocato una volta dopo il restore.
- photo review guard eseguito senza propagare ReferenceError.
PASS

TEST XR TRACER
- requestSession originale chiamata una sola volta.
- stessa sessione restituita.
- xr-depth-restore-requested emesso dopo xr-requestSession-ok.
- restore hook D13 invocato una sola volta con reason=xr-session-created.
- session.end wrapper continua a funzionare.
PASS

LIMITI
- Non e' stato possibile eseguire il vero WebXR immersivo nel container.
- Il vero modello Q4F16/ORT e' gia' stato verificato sul telefono nelle prove D10-D12; D13 non modifica il worker oltre ai nomi build.
- Il guard v10RenderPhotoReview e' intenzionalmente temporaneo: la preview foto resta bypassata finche' il bug rgb della V10 base non viene corretto con il sorgente completo.
