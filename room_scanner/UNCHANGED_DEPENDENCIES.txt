Room Scanner V12.2.0 keeps the verified Depth Anything runtime/model dependencies unchanged.
Do not remove or replace these unless intentionally upgrading the inference runtime:

  depth_ai_worker.js
  models/depth_anything_v2_small_q4.onnx
  vendor/depthai-123/

V12.2.0 changes the guided acquisition/model layer plus service-worker/build metadata.
It does not require SAM/MobileSAM. Deep inference is batch-only after the metric floor perimeter is confirmed.
