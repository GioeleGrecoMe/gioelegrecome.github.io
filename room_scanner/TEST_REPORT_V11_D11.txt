V11-D11 XR TRACE - TEST REPORT

Scopo: non cambiare Depth D10 che ha gia superato init/smoke/infer WebGPU. Aggiungere solo diagnostica lifecycle WebXR e un controllo opzionale ?depthoff=1.

Verifiche previste:
- sintassi di loader, xrtrace, diag, worker;
- XR trace iniettato all'inizio di <head>, prima degli script V10;
- worker D11 sostituito una sola volta;
- diagnostica D11 a fine body;
- ?depthoff=1 modifica solo depthAIEnabled:true -> false quando l'anchor esiste;
- nessun cambiamento a WebXR options, geometry, audio, camera, fusion;
- wrapper requestSession preserva argomenti, risultato ed errori;
- session.end wrapper richiama l'originale e registra soltanto il caller.

RISULTATI ESEGUITI NEL CONTAINER:
PASS node --check loader
PASS node --check xrtrace
PASS node --check diag
PASS node --check Depth worker
PASS loader transform normale
PASS loader transform ?depthoff=1
PASS XR trace: requestSession argomenti/risultato preservati
PASS XR trace: userActivation catturata
PASS XR trace: session.end caller + end event distinti
PASS worker D10->D11 identico dopo normalizzazione nomi build
