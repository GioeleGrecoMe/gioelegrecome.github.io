import {gaussianSnapshotToPly,parsePly,makeR30,parseR30} from '../js/formats.js';const a=new Float32Array(16*3);for(let i=0;i<3;i++){const s=i*16;a[s]=i*.5;a[s+1]=1+i*.1;a[s+2]=-i*.2;a[s+3]=a[s+4]=a[s+5]=.02;a[s+6]=0;a[s+7]=1;a[s+8]=0;a[s+9]=i/3;a[s+10]=.5;a[s+11]=1-i/3;a[s+12]=.7;}const snap={data:a,stride:16,count:3};const ply=gaussianSnapshotToPly(snap);const p=await parsePly(ply);if(p.count!==3||Math.abs(p.data[16]-.5)>.001)throw new Error('PLY roundtrip');const jpeg=new Blob([new Uint8Array([1,2,3,4])],{type:'image/jpeg'}),r30=makeR30({manifest:{id:'test'},keyframes:[{id:'kf-1',blob:jpeg}],gaussians:snap,imu:[{t:1}]});const r=await parseR30(r30);if(r.meta.id!=='test'||r.gaussians?.count!==3||!r.entries['keyframes/kf-1.jpg'])throw new Error('R30 roundtrip');console.log('PASS format_roundtrip');
