import fs from 'node:fs';const bytes=fs.readFileSync(new URL('../wasm/slam_core.wasm',import.meta.url));const {instance}=await WebAssembly.instantiate(bytes,{}),e=instance.exports,w=128,h=96;const mem=new Uint8Array(e.memory.buffer,e.input_ptr(),w*h);for(let y=0;y<h;y++)for(let x=0;x<w;x++)mem[y*w+x]=((x*17+y*11)^((x>>3)*53)^((y>>3)*29))&255;const a=e.process_frame(w,h,250,12);for(let y=0;y<h;y++)for(let x=0;x<w;x++){const xx=Math.max(0,x-3);mem[y*w+x]=((xx*17+y*11)^((xx>>3)*53)^((y>>3)*29))&255;}const b=e.process_frame(w,h,250,12),m=e.match_count();if(a<10||b<10||m<3)throw new Error(`weak WASM features ${a}/${b}, matches ${m}`);console.log(`PASS wasm_frontend features=${b} matches=${m}`);
