import http from 'node:http';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(new URL('..',import.meta.url).pathname);const mime={'.html':'text/html','.js':'text/javascript','.wasm':'application/wasm','.css':'text/css','.json':'application/json','.webmanifest':'application/manifest+json','.svg':'image/svg+xml'};const server=http.createServer((req,res)=>{let p=new URL(req.url,'http://x').pathname;if(p==='/')p='/index.html';const f=path.join(root,p);fs.readFile(f,(e,d)=>{if(e){res.statusCode=404;return res.end()}res.setHeader('Content-Type',mime[path.extname(f)]||'application/octet-stream');res.end(d);});});await new Promise(r=>server.listen(0,'127.0.0.1',r));const port=server.address().port;for(const f of ['/','/room_scanner_v30.html','/js/app.js','/js/slam/math.js','/wasm/slam_core.wasm','/workers/gaussian_worker.js','/workers/mesh_worker.js','/sw.js']){const r=await fetch(`http://127.0.0.1:${port}${f}`);if(!r.ok)throw new Error(`${f} HTTP ${r.status}`);if(f.endsWith('.wasm')&&!r.headers.get('content-type')?.includes('application/wasm'))throw new Error('bad wasm MIME');}server.close();console.log('PASS http_smoke');
