import {fitPlaneRansac,intrinsicsFromSize,median,triangulateRays,poseIdentity} from '../js/slam/math.js';
if(median([3,1,2,9])!==2.5)throw new Error('median');const pts=[];for(let x=-2;x<=2;x+=.2)for(let z=.5;z<=3;z+=.25)pts.push([x,1.3+(Math.sin(x*17+z)*.002),z]);pts.push([7,8,9],[-5,3,9]);const p=fitPlaneRansac(pts,{iterations:100,threshold:.02});if(!p||p.count<200)throw new Error('plane fit');const K={fx:300,fy:300,cx:160,cy:120,width:320,height:240},r=triangulateRays({pose:poseIdentity(),K,u:160,v:120},{pose:{p:[.2,0,0],q:[0,0,0,1]},K,u:130,v:120},{minAngleRad:.005,maxGapM:.05});if(!r.ok||Math.abs(r.p[2]-2)>.08)throw new Error(`triangulation ${JSON.stringify(r)}`);if(!(intrinsicsFromSize(320,240).fx>200))throw new Error('intrinsics');console.log('PASS math_triangulation_plane');
