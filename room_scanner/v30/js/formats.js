/** Offline import/export helpers for PLY Gaussian points and compact .r30 JSON. */
export function gaussiansToPly(items,comment='Room Scanner V30'){const gs=normalize(items),lines=['ply','format ascii 1.0',`comment ${comment}`,`element vertex ${gs.length}`,'property float x','property float y','property float z','property uchar red','property uchar green','property uchar blue','property float opacity','property float scale','end_header'];for(const g of gs)lines.push(`${g.p[0]} ${g.p[1]} ${g.p[2]} ${g.c[0]} ${g.c[1]} ${g.c[2]} ${g.a} ${g.r}`);return lines.join('\n')+'\n';}
export function parsePly(text){const lines=String(text).split(/\r?\n/),end=lines.findIndex(x=>x.trim()==='end_header');if(end<0)throw new Error('PLY end_header missing');const props=lines.slice(0,end).filter(x=>x.startsWith('property ')).map(x=>x.trim().split(/\s+/).pop()),out=[];for(const line of lines.slice(end+1)){if(!line.trim())continue;const v=line.trim().split(/\s+/).map(Number);if(v.length<3)continue;const obj={};props.forEach((p,i)=>obj[p]=v[i]);out.push({position:[obj.x,obj.y,obj.z],color:[obj.red??180,obj.green??180,obj.blue??180],opacity:obj.opacity??1,radius:obj.scale??.02});}return out;}
export function encodeR30(bundle){return new Blob([JSON.stringify({format:'ROOMSCAN-R30-JSON-1',...bundle})],{type:'application/octet-stream'});}
export async function decodeR30(file){const text=await file.text(),x=JSON.parse(text);if(!x||typeof x!=='object')throw new Error('R30 invalid');return x;}
export function downloadBlob(blob,name){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1500);}
function normalize(items){return (items||[]).map(g=>{const p=g.position||g.p||g.mean||g.xyz||[0,0,0],c=g.color||g.rgb||[180,210,240],s=g.scale||g.scales||g.radius||.02;return {p:p.map(Number),c:c.map(v=>Math.max(0,Math.min(255,Math.round(Number(v)||0)))),a:Number(g.opacity??g.alpha??1),r:Array.isArray(s)?Math.max(...s.map(Number)):Number(s)}}).filter(g=>g.p.every(Number.isFinite));}
