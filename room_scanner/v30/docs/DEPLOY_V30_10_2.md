# Deploy V30.10.2

Replace `room_scanner/v30/` with the `v30/` directory in this archive. Open `boot_diagnostic.html`, then open `room_scanner_v30.html?v30reset=1`. Primary controls must be clickable immediately. Run self-test and verify `interactive-boot-order`, `indexeddb`, `world-anchor-source`, and `build-info-fresh`. `Forza aggiornamento` unregisters V30 service workers and deletes V30 caches before reloading.
